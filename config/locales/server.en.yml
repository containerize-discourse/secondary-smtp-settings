en:
  site_settings:
    smtp_enabled: "Enable secondary smtp settings (Use this settings over Global smtp settings"
    smtp_address: "address of secondary smtp server used to send emails"
    smtp_port: "port of secondary smtp server used to send emails"
    smtp_domain: "domain passed to secondary smtp server"
    smtp_user_name: "username for secondary smtp server"
    smtp_password: "password for secondary smtp server"
    smtp_authentication: "secondary smtp authentication mechanism"
    smtp_enable_start_tls: "enable TLS encryption for secondary smtp connections"
    smtp_openssl_verify_mode: "mode for verifying secondary smtp server certificates"